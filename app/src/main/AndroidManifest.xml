<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.kritika.ayucompanion">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:icon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AyuCompanion">
        <activity android:name=".MainActivity"
            android:theme="@style/Theme.AyuCompanion">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Info"> </activity>
        <activity android:name=".Prakriti"/>
        <activity android:name=".food.Food"/>
        <activity android:name=".Meditation"/>
        <activity android:name=".yoga.Yoga"/>
        <activity android:name=".vegetable"/>
        <activity android:name=".Fruits"/>
        <activity android:name=".Oils"/>
        <activity android:name=".Spices"/>
        <activity android:name=".food.Dairy"/>


        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />


    </application>

</manifest>